function printNumbersWithStep(N, step) {
  for (let i = 1; i <= N; i += step) {
    console.log(i);
  }
}

// Приклад використання функції
printNumbersWithStep(7, 3); // Виведе числа від 1 до 7 з кроком 3
